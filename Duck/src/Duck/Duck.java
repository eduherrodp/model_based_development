package Duck;

// Implementing the class Duck
public class Duck {
    // Attributes FlyBehavior:fb and QuackBehavior:qb
    FlyBehavior fb;
    QuackBehavior qb;

    // Methods performQuack, swim, display, showDuck, setQuackBehavior, setFlyBehavior
    
}
