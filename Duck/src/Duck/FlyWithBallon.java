package Duck;

public class FlyWithBallon implements FlyBehavior {
    // This is a class that implements the interface FlyBehavior
    public void fly() {
        System.out.println("I'm flying with a balloon");
    }
}
